/*
float stack header file
*/
#ifndef STACK_FLOAT_H
#define STACK_FLOAT_H
#include "list_float.h"

extern void push(Node**, float);  //  Push an element onto the stack
extern void pop(Node**);  //  Pop off the top element of the stack

#endif
